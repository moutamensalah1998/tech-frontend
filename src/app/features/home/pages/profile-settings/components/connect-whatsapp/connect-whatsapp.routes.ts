import { Route } from '@angular/router';
import { ConnectWhatsappComponent } from './connect-whatsapp.component';

export const connectWhatsappRoute: Route = {
  path: 'connect-whatsapp',
  component: ConnectWhatsappComponent,
};

